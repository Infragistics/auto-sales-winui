<?xml version="1.0" encoding="utf-8"?>
<ResourceDictionary
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:local="using:AutoSales.Views">


    <Color x:Key="Primary100">#FFd0d1f8</Color>
    <Color x:Key="Primary500">#FF6366f1</Color>
    <Color x:Key="Primary600">#FF4B4ED8</Color>

    <Color x:Key="Gray100">#FFF0F1F4</Color>
    <Color x:Key="Gray200">#FFE2E2E9</Color>
    <Color x:Key="Gray300">#FFC5C5D3</Color>

    <Color x:Key="Gray500">#FF67687E</Color>
    <Color x:Key="Gray600">#FF585874</Color>
    <Color x:Key="Gray700">#FF3C3C49</Color>
    <Color x:Key="Gray800">#FF2E2E38</Color>
    <Color x:Key="Gray900">#FF0f1729</Color>

    <!-- App color palette (matches WPF showcase, no retemplating) -->
    <SolidColorBrush x:Key="AppHeaderBrush" Color="{StaticResource Gray900}" />
    <SolidColorBrush x:Key="AppHeaderForegroundBrush" Color="White" />
    <SolidColorBrush x:Key="AppHeaderForegroundBrushSubtle" Color="{StaticResource Gray300}" />
    
    <SolidColorBrush x:Key="AppBackgroundBrush" Color="{StaticResource Gray100}" />
    <SolidColorBrush x:Key="AppPanelBackgroundBrush" Color="White" />

    <SolidColorBrush x:Key="PrimaryBrush" Color="{StaticResource Primary500}" />
    <SolidColorBrush x:Key="PrimaryBrushDarker" Color="{StaticResource Primary600}" />
    <SolidColorBrush x:Key="PrimaryBrushLightest" Color="{StaticResource Primary100}" />

    <SolidColorBrush x:Key="AppRegionFillBrush" Color="#FFD3D4DE" />
    <SolidColorBrush x:Key="AppRegionStrokeBrush" Color="White" />
    <SolidColorBrush x:Key="AppGridLineBrush" Color="{StaticResource Gray200}" />

    <SolidColorBrush x:Key="AppTooltipBackgroundBrush" Color="{StaticResource Gray900}" />
    <SolidColorBrush x:Key="AppTooltipForegroundBrush" Color="White" />

    <SolidColorBrush x:Key="ForegroundBrushPrimary" Color="{StaticResource Gray700}" />
    <SolidColorBrush x:Key="ForegroundBrushPrimaryHover" Color="{StaticResource Gray800}" />
    <SolidColorBrush x:Key="ForegroundBrushSecondary" Color="{StaticResource Gray600}" />
    <SolidColorBrush x:Key="ForegroundBrushSubtle" Color="{StaticResource Gray500}" />

    <SolidColorBrush x:Key="SegmentedButtonContainer" Color="{StaticResource Gray200}" />
    <SolidColorBrush x:Key="AvatarBackground" Color="White" />

    <SolidColorBrush x:Key="MarkerBrush" Color="{StaticResource Primary500}" />
    <SolidColorBrush x:Key="MarkerOutline" Color="{StaticResource Primary500}" />

    <SolidColorBrush x:Key="CardBackground" Color="White" />
    <SolidColorBrush x:Key="CardBorderBrush" Color="{StaticResource Gray200}" />


    <!-- Region/state shape gray ramp keyed by RevenuePercent buckets -->
    <SolidColorBrush x:Key="ShapeFill10" Color="{StaticResource Gray100}" />
    <SolidColorBrush x:Key="ShapeFill20" Color="{StaticResource Gray200}" />
    <SolidColorBrush x:Key="ShapeFill30" Color="{StaticResource Gray300}" />
    <SolidColorBrush x:Key="ShapeFill40" Color="#FFB0B0C8" />
    <SolidColorBrush x:Key="ShapeFill50" Color="#FF9494AE" />
    <SolidColorBrush x:Key="ShapeFill60" Color="{StaticResource Gray500}" />
    <SolidColorBrush x:Key="ShapeFill70" Color="{StaticResource Gray600}" />
    <SolidColorBrush x:Key="ShapeFill80" Color="{StaticResource Gray700}" />
    <SolidColorBrush x:Key="ShapeFill90" Color="{StaticResource Gray800}" />
    <SolidColorBrush x:Key="ShapeFill100" Color="{StaticResource Gray900}" />

    <CornerRadius x:Key="CardCornerRadius">13</CornerRadius>
    <Thickness x:Key="CardMargin">16</Thickness>
    <Thickness x:Key="CardBorderThickness">1</Thickness>
    <Thickness x:Key="CardPadding">16</Thickness>

    <CornerRadius x:Key="SegmentedBtnContainerRadius">10</CornerRadius>

    <Thickness x:Key="LargeGap">24</Thickness>
    <Thickness x:Key="MediumGap">16</Thickness>
    <Thickness x:Key="SmallGap">8</Thickness>

    <!-- Segmented ToggleButton style used for Period (Year/Quarter/Month/Week)
         and Measure (Revenue/Volume) toggle groups. -->
    <Style x:Key="SegmentedToggleButtonStyle" TargetType="ToggleButton">
        <Setter Property="Background" Value="Transparent" />
        <Setter Property="BorderBrush" Value="Transparent" />
        <Setter Property="Foreground" Value="#FF666666" />
        <Setter Property="Padding" Value="14,6" />
        <Setter Property="FontSize" Value="13" />
        <Setter Property="FontWeight" Value="Normal" />
        <Setter Property="CornerRadius" Value="10" />
        <Setter Property="MinWidth" Value="0" />
        <Setter Property="Template">
            <Setter.Value>
                <ControlTemplate TargetType="ToggleButton">
                    <Grid>
                        <VisualStateManager.VisualStateGroups>
                            <VisualStateGroup x:Name="CommonStates">
                                <!-- Default / unchecked -->
                                <VisualState x:Name="Normal" />

                                <VisualState x:Name="PointerOver">
                                    <VisualState.Setters>
                                        <Setter Target="Root.Background" Value="#66CCCCCC" />
                                    </VisualState.Setters>
                                </VisualState>

                                <VisualState x:Name="Pressed">
                                    <VisualState.Setters>
                                        <Setter Target="Root.Background" Value="#66CCCCCC" />
                                    </VisualState.Setters>
                                </VisualState>

                                <!-- Checked (active segment) -->
                                <VisualState x:Name="Checked">
                                    <VisualState.Setters>
                                        <Setter Target="Root.Background" Value="{StaticResource PrimaryBrush}" />
                                        <Setter Target="Root.BorderBrush" Value="{StaticResource PrimaryBrush}" />
                                        <Setter Target="Content.Foreground" Value="White" />
                                        <Setter Target="Content.FontWeight" Value="SemiBold" />
                                    </VisualState.Setters>
                                </VisualState>

                                <VisualState x:Name="CheckedPointerOver">
                                    <VisualState.Setters>
                                        <Setter Target="Root.Background" Value="{StaticResource PrimaryBrushDarker}" />
                                        <Setter Target="Root.BorderBrush" Value="{StaticResource PrimaryBrushDarker}" />
                                        <Setter Target="Content.Foreground" Value="White" />
                                        <Setter Target="Content.FontWeight" Value="SemiBold" />
                                    </VisualState.Setters>
                                </VisualState>

                                <VisualState x:Name="CheckedPressed">
                                    <VisualState.Setters>
                                        <Setter Target="Root.Background" Value="{StaticResource PrimaryBrushDarker}" />
                                        <Setter Target="Root.BorderBrush" Value="{StaticResource PrimaryBrushDarker}" />
                                        <Setter Target="Content.Foreground" Value="White" />
                                        <Setter Target="Content.FontWeight" Value="SemiBold" />
                                    </VisualState.Setters>
                                </VisualState>

                                <VisualState x:Name="Disabled">
                                    <VisualState.Setters>
                                        <Setter Target="Root.Background" Value="#FFF4F4F5" />
                                        <Setter Target="Root.BorderBrush" Value="{StaticResource AppGridLineBrush}" />
                                        <Setter Target="Content.Foreground" Value="#FFAAAAAA" />
                                        <Setter Target="Root.Opacity" Value="0.5" />
                                    </VisualState.Setters>
                                </VisualState>
                            </VisualStateGroup>
                        </VisualStateManager.VisualStateGroups>

                        <Border x:Name="Root"
                                Background="{TemplateBinding Background}" 
                                BorderBrush="{TemplateBinding BorderBrush}"
                                BorderThickness="{TemplateBinding BorderThickness}"
                                CornerRadius="{TemplateBinding CornerRadius}"
                                Padding="{TemplateBinding Padding}">
                            <ContentPresenter x:Name="Content"
                                              Content="{TemplateBinding Content}"
                                              HorizontalAlignment="Center"
                                              VerticalAlignment="Center"
                                              Foreground="{TemplateBinding Foreground}" />
                        </Border>
                    </Grid>
                </ControlTemplate>
            </Setter.Value>
        </Setter>
    </Style>

    <Style x:Key="CardTitleTextStyle" TargetType="TextBlock">
        <Setter Property="FontSize" Value="16"/>
        <Setter Property="FontWeight" Value="SemiBold"/>
        <Setter Property="Foreground" Value="{ThemeResource ForegroundBrushPrimary}"/>
        <Setter Property="VerticalAlignment" Value="Center"/>
    </Style>

    <Style x:Key="SmallTextSemibold" TargetType="TextBlock">
        <Setter Property="FontSize" Value="13"/>
        <Setter Property="FontWeight" Value="SemiBold"/>
        <Setter Property="Foreground" Value="{ThemeResource ForegroundBrushSecondary}"/>
        <Setter Property="VerticalAlignment" Value="Center"/>
    </Style>

    <Style x:Key="SmallTextStyle" TargetType="TextBlock">
        <Setter Property="FontSize" Value="13"/>
        <Setter Property="Foreground" Value="{ThemeResource ForegroundBrushSecondary}"/>
        <Setter Property="VerticalAlignment" Value="Center"/>
    </Style>

</ResourceDictionary>
